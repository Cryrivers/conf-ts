# Shared build output for compiler-native, macro-transformer-core,
# macro-transformer-native, and swc-plugin. Cargo.lock is intentionally
# committed at this workspace root so all SWC entry points stay in sync.
/target
